// // import BaseButton from './BaseButton';
import BaseButton from './BaseButton';
import PillButton from './PillButton';

function add(a, b) {
    return a + b;
}

export default add;

export { BaseButton, PillButton };
